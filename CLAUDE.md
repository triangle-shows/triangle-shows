# triangle-shows — Claude Instructions

## Deploying changes

After implementing a fix or feature, use `/deploy` rather than running git commands manually. The `/deploy` skill commits, pushes, and waits in the background for the Cloud Run deploy to go live, then notifies you when it's up.
